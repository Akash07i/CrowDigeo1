import React from "react";
import { Typography } from "@mui/material";

export default function PushersPage() {
  return (
    <div>
      <Typography variant="h4">Pushers</Typography>
      <Typography variant="body1">Manage documents, deals, and notifications with Pushers.</Typography>
    </div>
  );
}
