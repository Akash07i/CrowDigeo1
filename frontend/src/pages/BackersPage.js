import React from "react";
import { Typography } from "@mui/material";

export default function BackersPage() {
  return (
    <div>
      <Typography variant="h4">Backers</Typography>
      <Typography variant="body1">Approve perks, chat, and personalize messages.</Typography>
    </div>
  );
}
